# Vercel's Python runtime resolves requirements.txt relative to the
# project's Root Directory, which is frontend/. The real dependency list
# lives in backend/requirements.txt, one level up from here.
-r ../backend/requirements.txt
